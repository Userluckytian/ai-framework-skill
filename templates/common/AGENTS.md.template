# Git 提交规范

## 提交信息格式

```
<gitmoji><type>(<scope>): <中文描述>
```

### type 类型

| type | 说明 | gitmoji |
|------|------|---------|
| feat | 新功能 | ✨ |
| fix | 修复 bug | 🐛 |
| docs | 文档更新 | 📝 |
| style | 代码格式（不影响功能） | 🎨 |
| refactor | 重构（非修复或新增） | ♻️ |
| test | 添加或修改测试 | ✅ |
| chore | 构建/工具变动 | 🔧 |
| perf | 性能优化 | ⚡ |
| ci | CI/CD 配置 | 🐳 |
| revert | 回滚 | ⏪ |

### scope 范围

项目模块名，如 `auth`、`api`、`login`、`layout`、`theme`、`i18n` 等。

### 规则

- 描述使用**中文**，祈使语气，首字母不大写，结尾不加句号
- 首行不超过 50 个字符
- 正文每行不超过 72 个字符
- 可引用 issue：`Related issue: #123`

### 示例

```
✨feat(auth): 添加用户注册接口

🐛fix(login): 修复登录报错弹窗重复显示

♻️refactor(api): 请求拦截器支持 showGlobalError 开关
```

## 分支命名

```
<type>/<task-id>-<description>
```

- `feature/123-login-api`
- `bugfix/header-styling`
- `hotfix/fix-crash`
- `chore/update-deps`

## 提交前代码审查

在每次执行 `git commit` 之前，必须主动询问用户是否需要进行代码审查。
- 若用户选择"是"：
  1. 执行 `/review` 综合审查命令
  2. 或按 `CODE_REVIEW.md` 中的审查要点逐项检查
  3. 可单独调用子代理深度检查：
     - `@code-stylespector` — 代码规范检查
     - `@test-engineer` — 测试运行
     - `@dependencies-checker` — 依赖检查
     - `@project-auditor` — 架构审计
  4. 审查通过后再提交
- 若用户选择"否"，则直接进入提交流程

## AI 子代理与命令

本项目配置了以下 AI 子代理，可通过 `@子代理名` 调用：

| 子代理 | 命令 | 职责 |
|--------|------|------|
| test-engineer | `/test` | 编写单元测试、运行测试、分析覆盖率 |
| project-auditor | `/audit` | 项目自检、架构审计、升级建议 |
| dependencies-checker | `/deps` | 依赖过期/安全漏洞/未使用依赖检查 |
| code-stylespector | `/style` | 代码风格检查（对照coding-standards.md） |

综合审查命令：`/review` — 依次调用上述子代理，输出提交建议。
