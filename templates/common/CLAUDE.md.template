# {{PROJECT_NAME}} 项目指令

## 阶段化协作（若已安装 OpenCode / ai-framework）

大块功能按 `docs/ai-framework/phased-plan-driven.md`：**计划 → 交接提示词 → 执行 → 证据验收**。  
计划目录：`docs/ai-framework/plans/` 或 `docs/superpowers/plans/`。  
单次提交审查用 `CODE_REVIEW.md`；整阶段验收输出四段结论（基线/对照表/结论/下一步）。

## Git 提交规范

提交信息格式：`<gitmoji><type>(<scope>): <中文描述>`

- type: feat / fix / docs / style / refactor / test / chore / perf / ci / revert
- scope: 项目模块名（auth / api / login / layout / theme / i18n 等）
- 描述使用中文，祈使语气，首字母不大写，结尾不加句号
- 首行不超过 50 字符，正文每行不超过 72 字符
- 默认不 `git push`，除非人类明确要求

示例：
```
✨feat(auth): 添加用户注册接口
🐛fix(login): 修复登录报错弹窗重复显示
♻️refactor(api): 请求拦截器支持 showGlobalError 开关
```

## 代码规范

- 以仓库内 `coding-standards.md` / `architecture.md` 为准（若存在）
- 最小修改原则：不推倒重构，只改动必要部分
- 密钥与真实凭证不进 git；验收认命令输出不认「应该能过」

## 按天问题日志（强制）

处理任何问题/需求时，同步维护 `docs/issue-log/YYYY-MM-DD.md`（描述 / 分析 / 修改结果 / 状态）；修复后更新状态为「已关闭」并附验证证据；未关闭项次日自动带过。约定详见 `docs/issue-log/README.md`。
