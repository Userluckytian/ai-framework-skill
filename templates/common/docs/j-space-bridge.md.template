# J-Space × 阶段化计划驱动：桥接说明（Seam）

**项目**: {{PROJECT_NAME}}
**用途**: 让本仓库的**项目过程规则**（`AGENTS.md`、阶段化计划驱动）与可选的 **J-Space 推理时控制**（`.grok/skills/j-space/`）对接。两套规则分层运行、共用一张门控表。

**本文件是 seam，不是第 N 套规则**：过程规则和推理协议都不修改，只定义它们的对接点。

---

## 1. 分层边界

| 层 | 规则在哪 | 管什么 | 状态写在哪 |
|----|----------|--------|-----------|
| **过程层（A，本仓库）** | `AGENTS.md`、`docs/ai-framework/phased-plan-driven.md`、`CODE_REVIEW.md` | 任务怎么组织：分级、计划、交接、验收、提交审查 | 仓库：plans、commit 链、验收表 |
| **推理层（B，J-Space）** | `.grok/skills/j-space/`（如已安装） | 模型怎么思考：加载什么、保持什么、在哪验证、何时收束 | 推理工作区：Inner / Ledger / Outer |

**不相交也不冲突**：A 把状态写在仓库（跨会话持久）；B 把状态写在推理工作区（任务内）。两层只通过下面的门控表和 seam 对接。

## 2. 共用门控表（先判定，再走仪式）

每个任务先按变更规模选一档；**仪式只对档位负责**，小任务禁止跑全量：

| 档位 | 判定标准 | A（过程层）要求 | B（J-Space 对应） |
|------|----------|-----------------|-------------------|
| **S 小改** | 一眼可核验：typo、重命名、单文件无逻辑改动、文档措辞 | 零仪式：无计划、无 `/review` 套件；仅 diff 目检 | `fast`：不加载任何模块，直接回答 |
| **M 中改** | 2–4 文件、单个交付物、有逻辑改动 | 针对性测试（只跑受影响模块）+ `/review` 仅限 diff 文件 | `full`：加载该任务命中的一两个模块 |
| **L 大改** | 跨文件、跨阶段、跨会话、长时程 | 阶段计划 + 验收表 + 全量验证（四件套缺一不可） | `loop`：账本 + seam 刷新 + 检查点 + 恢复 |

**硬规则**：
- S 级任务**不进阶段流**，不触发 `/plan-phase`、`/accept-phase`，不跑 `/review` 套件。
- M 级**不进阶段流**：`/review` 只审本次 diff，测试只跑受影响范围。
- L 级才允许使用完整的阶段化计划驱动流程。
- 任何档位都不允许把 J-Space 的稠密标记（`✓`/`?`/`✗`）写进用户可见输出或 commit 消息。

## 3. seam 对接点

两套协议在以下位置重合，重合时按本表执行：

| 时刻 | 过程层动作（A） | 推理层动作（B） |
|------|----------------|-----------------|
| 阶段/任务开始 | `/plan-phase`（L 级）或口头定界（S/M 级） | 按档位给 pass：`fast`/`full`/`loop`；L 级打开账本 |
| 交接 | `/handoff` 生成交接提示词 | 交接点是 **seam**：刷新 Ledger（目标/已验证/开放/下一步），Inner 转 Outer 后交付 |
| 执行中 | 按 Task 推进、小步 commit | seam 处审计：账本每缝刷新；前提与不变量每三缝刷新 |
| 自证/验收 | 执行方跑测试/构建并自评验收表；`/accept-phase` 对照验收 | `ship` 检查（外层寄存器审查）在交付/提交前执行 |
| 提交前 | `/review`（M 级 diff 审查）或 `CODE_REVIEW.md` 目检（S 级） | 提交点是 **seam**；B 的 `ship` 与 A 的 `/review` 是同一道门，不重复执行 |
| 长间隔后 | `resume` / 读 plans 恢复现场 | `jspace.py resume` 或手读账本 + 前提 + 不变量，指定 Next |

## 4. 词汇互译（不是两套术语，是同一件事的两面）

| 过程层说法（A） | 推理层说法（B） |
|----------------|-----------------|
| 验收表一行（基线 + 证据） | checkpoint（已验证 + 验证者 + 覆盖范围） |
| 验证不通过项下放下一阶段 | 未验证状态标注（`?`）继续持有，直到有验证者 |
| 交接提示词（零上下文） | Outer 寄存器（干净、完整、无稠密符号） |
| 红线（密钥不进 git 等） | 不变量（seam 处检查） |
| 独立验收（对照表复跑） | 元认知控制（信号必须选择动作，否则只是评论） |

## 5. 安装与启用

- 本仓库安装了 `j-space` 选项后（`--tool jspace`），J-Space 落在 `<repo>/.grok/skills/j-space/`，随仓库走。
- **默认启用**：安装同时写入 `<repo>/.grok/rules/j-space-default.md`（Grok 常驻规则），所有任务自动按 J-Space 门控执行，**无需显式 `/j-space`**。
- 未使用 Grok 的宿主：在任务开头说"使用 j-space 推理协议"，或按 `j-space-default.md` 内容把常驻规则加入宿主规则文件。
- 未安装 j-space 时，本文件的门控表仍然有效（S/M/L 分级独立成立），只是推理层行忽略。